"""Protocol / API layer for the Honeywell Smile Connect gateway.

This package has no Home Assistant dependencies by design, so it can be
tested and iterated on independently (and potentially split out into its
own PyPI package later, under this project's own name).
"""
